{
  "name": "Flat-Adaptive Radial Poisson Reconstruction Law",
  "equation": "\\pi_f(r)=\\pi_f(r_*)\\exp\\!\\left(\\int_{r_*}^{r}\\left[\\frac{f(s)-u''(s)}{u'(s)}-\\frac{1}{s}\\right]ds\\right),\\quad \\pi_f(r)=\\pi_f(r_*)\\frac{r_*u'(r_*)}{r u'(r)}\\exp\\!\\left(\\int_{r_*}^{r}\\frac{f(s)}{u'(s)}\\,ds\\right)",
  "description": "Exact inverse-recovery law for the flat-adaptive radial Poisson branch on monotone intervals. Starting from the general radial operator Delta_f^rad u = u'' + ((1/r) + pi_f'/pi_f) u', the branch pi_f is reconstructed explicitly from measured u and known forcing f wherever u' does not vanish. The theorem determines pi_f up to one multiplicative calibration constant, exposes an equivalent shell-flux form, reduces exactly to the harmonic-flux inversion when f=0, and yields a direct power-law consistency diagnostic beta_obs(r)=r(f-u'')/u' - 1.",
  "source": "Adaptive Flat Pi",
  "submitter": "RDM3DC",
  "units": "OK",
  "theory": "PASS",
  "assumptions": [
    "On the recovery interval I, the measured profile satisfies u in C^2(I), the forcing satisfies f in C^0(I), and the branch obeys pi_f(r) > 0.",
    "The reconstruction is carried out only on subintervals where u'(r) does not vanish and keeps fixed sign, so division by u' and the logarithmic integration step are well-defined.",
    "The radial operator is the same flat-adaptive branch already used elsewhere in the registry: Delta_f^rad u = u'' + ((1/r) + pi_f'/pi_f) u'.",
    "Recovery determines pi_f only up to one multiplicative calibration constant; a separate datum such as one known pi_f(r_*), one shell-flux value, or an independent area observable is needed to fix absolute scale."
  ],
  "evidence": [
    "Exact algebraic lineage: solve the existing inverse law pi_f'/pi_f = (f-u'')/u' - 1/r by direct integration to obtain pi_f in quadrature form.",
    "Exact cleaner form: splitting the exponent into f/u', u''/u', and 1/r terms yields pi_f(r)=pi_f(r_*) (r_* u'(r_*))/(r u'(r)) exp(int f/u').",
    "Exact flux form: with w_f(r)=2 pi_f(r) r and J_f=w_f u', the Poisson equation becomes J_f'/J_f = f/u', so the same reconstruction follows from shell-flux evolution.",
    "Exact harmonic reduction: when f=0, the formula collapses to 2 pi_f(r) r u'(r) = constant, matching the harmonic-flux inversion law already implicit in the flat-adaptive annular branch.",
    "Exact power-law diagnostic: if pi_f(r)=pi lambda_0 (r/r_0)^beta, then beta_obs(r)=r(f(r)-u''(r))/u'(r)-1 must be constant on the interval.",
    "Identifiability boundary is explicit: at turning points u'(r_0)=0 the local equation only gives u''(r_0)=f(r_0), so the theorem correctly isolates the real instability as piecewise-monotone recovery rather than algebraic solvability."
  ],
  "animation": {
    "status": "planned",
    "path": ""
  },
  "image": {
    "status": "planned",
    "path": ""
  }
}